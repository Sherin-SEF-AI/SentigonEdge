# Mosquitto config for the Sentigon sensor plane (MQTT ingest bridge).
# Local-first dev: a single anonymous listener on 1883. For a real deploy, add
# per-device credentials (password_file) and/or TLS, and disable anonymous.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
